{
  "$schema": "rule_template",
  "template_type": "workflow",
  "description": "Template for workflow ordering and checkpoint rules",
  "fields": {
    "id": "rule_workflow_XXX",
    "name": "Workflow Rule Name",
    "category": "workflows",
    "type": "hard|soft",
    "priority": 1,
    "trigger_keywords": ["workflow-specific", "keywords"],
    "condition": {
      "when": "Description of workflow context",
      "workflow_type": "build|deploy|test|design|production"
    },
    "steps": {
      "required_order": ["step1", "step2", "step3"],
      "checkpoints": ["after_step1", "after_step2"],
      "can_skip": ["optional_step"]
    },
    "failure_handling": {
      "on_step_failure": "retry|abort|skip",
      "max_retries": 2,
      "rollback_to": "checkpoint_name"
    },
    "confidence": 0.7,
    "validations": 0,
    "created": "2026-02-15",
    "last_validated": null
  }
}
